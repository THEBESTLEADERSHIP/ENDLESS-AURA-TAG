local player = game.Players.LocalPlayer 
local runService = game:GetService("RunService") 
local userInputService = game:GetService("UserInputService") 
local Players = game:GetService("Players")
local starterGui = game:GetService("StarterGui")

-- ANIMATION CONFIGURATION
local bangAnimId = "rbxassetid://137182335630609"
local faceBangAnimId = "rbxassetid://91969343990570"
local activeTrack = nil
local animConnection = nil

local function getHumanoid()
    return player.Character and player.Character:FindFirstChildOfClass("Humanoid")
end

-- ====================================================================
-- // RE-EXECUTION CLEAN WIPE & RESTART SYSTEM // --
-- ====================================================================
if _G.EndlessAuraStorage then
    if _G.EndlessAuraStorage.CurrentGui then
        _G.EndlessAuraStorage.CurrentGui:Destroy()
    end
    if _G.EndlessAuraStorage.Connections then
        for _, conn in ipairs(_G.EndlessAuraStorage.Connections) do
            pcall(function() if conn then conn:Disconnect() end end)
        end
    end
    if _G.EndlessAuraStorage.AnimForceConnection then
        _G.EndlessAuraStorage.AnimForceConnection:Disconnect()
    end
    
    -- Reset FallenPartsDestroyHeight back to default on clean wipe
    pcall(function()
        game.Workspace.FallenPartsDestroyHeight = -500
    end)
    
    pcall(function()
        local myChar = player.Character
        if myChar then
            local hum = myChar:FindFirstChildOfClass("Humanoid")
            if hum then
                for _, track in pairs(hum:GetPlayingAnimationTracks()) do
                    track:Stop()
                end
                hum.PlatformStand = false
                hum:ChangeState(Enum.HumanoidStateType.Running)
            end
            local root = myChar:FindFirstChild("HumanoidRootPart")
            if sethiddenproperty and root then
                sethiddenproperty(root, "NetworkIsSleeping", false)
            end
            local torso = myChar:FindFirstChild("Torso") or myChar:FindFirstChild("LowerTorso")
            local rootJoint = torso and (torso:FindFirstChild("RootJoint") or myChar.HumanoidRootPart:FindFirstChild("RootJoint"))
            if rootJoint then rootJoint.C0 = CFrame.new() end
        end
    end)
    
    _G.EndlessAuraStorage = nil
    task.wait(0.1)
end

-- Initialize clean memory storage
_G.EndlessAuraStorage = {
    CurrentGui = nil,
    Connections = {},
    AnimForceConnection = nil
}

-- CONFIGURATIONS
local speedMultiplier = 17.4   
local forwardBackRange = 1.1   
local fixedUpOffset = 1.8      
local attachDistance = 0.4     

local targetMode = "Normal" 
local isAttached = false
local selectedPlayers = {} 
local currentTargetIndex = 1
local lastTargetSwitchTime = 0
local selectedPlayer = nil 

local currentAutoSubMode = "Normal"
local lastSwitchTime = 0
local TARGET_OFFSET = Vector3.new(0, 0, 1)

-- Helper to get reliable orientation part
local function getOrientationPart(char)
    if not char then return nil end
    return char:FindFirstChild("UpperTorso") or char:FindFirstChild("Torso") or char:FindFirstChild("HumanoidRootPart")
end

-- ANTIVOID ENGINE FUNCTIONS
local function enableAntivoid()
    pcall(function()
        game.Workspace.FallenPartsDestroyHeight = 0/0  -- NaN Protection Locked
    end)
end

local function disableAntivoid()
    pcall(function()
        game.Workspace.FallenPartsDestroyHeight = -500  -- Default Reset
    end)
end

-- ====================================================================
-- // PANEL GUI DESIGN // --
-- ====================================================================
local screenGui = Instance.new("ScreenGui") 
screenGui.Name = "DeltaGlassRoboticUI" 
screenGui.ResetOnSpawn = false 
screenGui.Parent = game.CoreGui 
_G.EndlessAuraStorage.CurrentGui = screenGui 

local mainFrame = Instance.new("Frame") 
mainFrame.Size = UDim2.new(0, 250, 0, 360) 
mainFrame.Position = UDim2.new(0.5, -125, 0.5, -180) 
mainFrame.BackgroundColor3 = Color3.fromRGB(0, 0, 0) 
mainFrame.BackgroundTransparency = 0.45 
mainFrame.BorderSizePixel = 2 
mainFrame.BorderColor3 = Color3.fromRGB(255, 0, 50) 
mainFrame.BorderMode = Enum.BorderMode.Inset 
mainFrame.ClipsDescendants = true 
mainFrame.Visible = true 
mainFrame.Parent = screenGui 
Instance.new("UICorner", mainFrame).CornerRadius = UDim.new(0, 4)

local nameBoxFrame = Instance.new("Frame")
nameBoxFrame.Size = UDim2.new(1, -12, 0, 36) 
nameBoxFrame.Position = UDim2.new(0, 6, 0, 6)
nameBoxFrame.BackgroundColor3 = Color3.fromRGB(15, 0, 5)
nameBoxFrame.BackgroundTransparency = 0.3
nameBoxFrame.BorderSizePixel = 1
nameBoxFrame.BorderColor3 = Color3.fromRGB(255, 0, 50)
nameBoxFrame.Parent = mainFrame

local brandingImg = Instance.new("ImageLabel")
brandingImg.Size = UDim2.new(0, 26, 0, 26)
brandingImg.Position = UDim2.new(0, 6, 0, 5)
brandingImg.Image = "rbxassetid://129299711445020"
brandingImg.BackgroundTransparency = 1
brandingImg.Parent = nameBoxFrame

local nameText = Instance.new("TextLabel")
nameText.Size = UDim2.new(1, -45, 1, 0) 
nameText.Position = UDim2.new(0, 38, 0, 0)
nameText.Text = "ENDLESS AURA BANG PANEL"
nameText.TextColor3 = Color3.fromRGB(255, 255, 255)
nameText.Font = Enum.Font.Code 
nameText.TextSize = 10.5
nameText.TextXAlignment = Enum.TextXAlignment.Left
nameText.BackgroundTransparency = 1
nameText.Parent = nameBoxFrame

local toggleGuiBtn = Instance.new("TextButton")
toggleGuiBtn.Size = UDim2.new(0, 70, 0, 30)
toggleGuiBtn.Position = UDim2.new(0, 15, 1, -45) 
toggleGuiBtn.BackgroundColor3 = Color3.fromRGB(5, 5, 5)
toggleGuiBtn.BackgroundTransparency = 0.3
toggleGuiBtn.BorderSizePixel = 2
toggleGuiBtn.BorderColor3 = Color3.fromRGB(255, 0, 50)
toggleGuiBtn.Text = "[ CLOSE ]"
toggleGuiBtn.TextColor3 = Color3.fromRGB(255, 0, 50)
toggleGuiBtn.Font = Enum.Font.Code
toggleGuiBtn.TextSize = 12
toggleGuiBtn.Parent = screenGui
Instance.new("UICorner", toggleGuiBtn).CornerRadius = UDim.new(0, 4)

toggleGuiBtn.MouseButton1Click:Connect(function()
    mainFrame.Visible = not mainFrame.Visible
    toggleGuiBtn.Text = mainFrame.Visible and "[ CLOSE ]" or "[ OPEN ]"
    toggleGuiBtn.TextColor3 = mainFrame.Visible and Color3.fromRGB(255, 0, 50) or Color3.fromRGB(0, 255, 150)
    toggleGuiBtn.BorderColor3 = mainFrame.Visible and Color3.fromRGB(255, 0, 50) or Color3.fromRGB(0, 255, 150)
end)

-- Draggable Engine
local dragging, dragStart, startPos
nameBoxFrame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = mainFrame.Position
    end
end)
userInputService.InputChanged:Connect(function(input)
    if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
        local delta = input.Position - dragStart
        mainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)
userInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then dragging = false end
end)

-- SEARCH BOX
local textBox = Instance.new("TextBox") 
textBox.Size = UDim2.new(1, -16, 0, 26) 
textBox.Position = UDim2.new(0, 8, 0, 50) 
textBox.PlaceholderText = "> SEARCH MULTI TARGETS..." 
textBox.Text = "" 
textBox.BackgroundColor3 = Color3.fromRGB(10, 10, 10) 
textBox.BackgroundTransparency = 0.4
textBox.TextColor3 = Color3.fromRGB(255, 255, 255) 
textBox.TextSize = 11
textBox.Font = Enum.Font.Code
textBox.BorderColor3 = Color3.fromRGB(150, 0, 30) 
textBox.Parent = mainFrame 

-- SCROLL LIST
local scrollingFrame = Instance.new("ScrollingFrame") 
scrollingFrame.Size = UDim2.new(1, -16, 0, 65) 
scrollingFrame.Position = UDim2.new(0, 8, 0, 82) 
scrollingFrame.BackgroundColor3 = Color3.fromRGB(5, 5, 5) 
scrollingFrame.BackgroundTransparency = 0.4
scrollingFrame.ScrollBarThickness = 3 
scrollingFrame.BorderColor3 = Color3.fromRGB(100, 0, 20) 
scrollingFrame.Parent = mainFrame 
local uiList = Instance.new("UIListLayout", scrollingFrame)
uiList.Padding = UDim.new(0, 2)

-- SIDE-BY-SIDE SMALL BUTTONS SETUP
local clearBtn = Instance.new("TextButton")
clearBtn.Size = UDim2.new(0.5, -12, 0, 22) 
clearBtn.Position = UDim2.new(0, 8, 0, 154) 
clearBtn.Text = "[ CLEAR ]"
clearBtn.BackgroundColor3 = Color3.fromRGB(25, 5, 10)
clearBtn.TextColor3 = Color3.fromRGB(255, 50, 50)
clearBtn.BorderColor3 = Color3.fromRGB(200, 0, 50)
clearBtn.Font = Enum.Font.Code
clearBtn.TextSize = 11
clearBtn.Parent = mainFrame

local selectAllBtn = Instance.new("TextButton")
selectAllBtn.Size = UDim2.new(0.5, -12, 0, 22) 
selectAllBtn.Position = UDim2.new(0.5, 4, 0, 154) 
selectAllBtn.Text = "[ SELECT ALL ]"
selectAllBtn.BackgroundColor3 = Color3.fromRGB(5, 20, 10)
selectAllBtn.TextColor3 = Color3.fromRGB(50, 255, 150)
selectAllBtn.BorderColor3 = Color3.fromRGB(0, 200, 100)
selectAllBtn.Font = Enum.Font.Code
selectAllBtn.TextSize = 11
selectAllBtn.Parent = mainFrame

-- TARGET MODE BUTTON
local modeBtn = Instance.new("TextButton")
modeBtn.Size = UDim2.new(1, -16, 0, 24)
modeBtn.Position = UDim2.new(0, 8, 0, 182)
modeBtn.Text = "MODE // SYSTEM_NORMAL"
modeBtn.BackgroundColor3 = Color3.fromRGB(15, 5, 5)
modeBtn.BackgroundTransparency = 0.3
modeBtn.TextColor3 = Color3.fromRGB(200, 200, 200)
modeBtn.Font = Enum.Font.Code
modeBtn.TextSize = 11
modeBtn.BorderColor3 = Color3.fromRGB(180, 0, 40)
modeBtn.Parent = mainFrame

modeBtn.MouseButton1Click:Connect(function()
    if targetMode == "Normal" then
        targetMode = "Face Bang"
        modeBtn.Text = "MODE // CORE_FACE_BANG"
        modeBtn.TextColor3 = Color3.fromRGB(255, 100, 100)
    elseif targetMode == "Face Bang" then
        targetMode = "Auto Switch"
        modeBtn.Text = "MODE // AUTO_SWITCH LOOP"
        modeBtn.TextColor3 = Color3.fromRGB(0, 200, 255)
    else
        targetMode = "Normal"
        modeBtn.Text = "MODE // SYSTEM_NORMAL"
        modeBtn.TextColor3 = Color3.fromRGB(200, 200, 200)
    end
end)

-- SLIDER GENERATOR
local function createSlider(minY, labelText, minVal, maxVal, defaultVal, callback)
    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(1, -16, 0, 14)
    label.Position = UDim2.new(0, 8, 0, minY)
    label.Text = "SYS_" .. labelText .. ":: " .. tostring(defaultVal)
    label.TextColor3 = Color3.fromRGB(180, 180, 180)
    label.Font = Enum.Font.Code
    label.TextSize = 9.5
    label.BackgroundTransparency = 1
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Parent = mainFrame

    local track = Instance.new("TextButton") 
    track.Size = UDim2.new(1, -16, 0, 4)
    track.Position = UDim2.new(0, 8, 0, minY + 14)
    track.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
    track.Text = ""
    track.AutoButtonColor = false
    track.BorderSizePixel = 0
    track.Parent = mainFrame

    local fill = Instance.new("Frame")
    fill.Size = UDim2.new((defaultVal - minVal)/(maxVal - minVal), 0, 1, 0)
    fill.BackgroundColor3 = Color3.fromRGB(255, 0, 50)
    fill.BorderSizePixel = 0
    fill.Parent = track

    local knob = Instance.new("Frame")
    knob.Size = UDim2.new(0, 8, 0, 10)
    knob.Position = UDim2.new((defaultVal - minVal)/(maxVal - minVal), -4, 0.5, -5)
    knob.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    knob.Parent = track

    local function updateSliderPosition(inputPositionX)
        local relativeX = math.clamp((inputPositionX - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
        knob.Position = UDim2.new(relativeX, -4, 0.5, -5)
        fill.Size = UDim2.new(relativeX, 0, 1, 0)
        
        local currentVal = math.floor((minVal + (relativeX * (maxVal - minVal))) * 10) / 10
        label.Text = "SYS_" .. labelText .. ":: " .. tostring(currentVal)
        callback(currentVal)
    end

    local sliderDragging = false
    track.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            sliderDragging = true
            updateSliderPosition(input.Position.X) 
        end
    end)
    userInputService.InputChanged:Connect(function(input)
        if sliderDragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
            updateSliderPosition(input.Position.X)
        end
    end)
    userInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then sliderDragging = false end
    end)
end

createSlider(212, "EMOTE_SPEED", 1, 40, speedMultiplier, function(v) 
    speedMultiplier = v 
    if activeTrack then activeTrack:AdjustSpeed(v / 5) end
end)
createSlider(242, "STROKE_RANGE", 0, 5, forwardBackRange, function(v) forwardBackRange = v end)
createSlider(272, "HEIGHT_OFFSET", -10, 10, fixedUpOffset, function(v) fixedUpOffset = v end)
createSlider(302, "ATTACH_DIST", 0, 15, attachDistance, function(v) attachDistance = v end)

local toggleButton = Instance.new("TextButton") 
toggleButton.Size = UDim2.new(1, -16, 0, 26) 
toggleButton.Position = UDim2.new(0, 8, 1, -32) 
toggleButton.Text = "[ FUCK ON ]" 
toggleButton.BackgroundColor3 = Color3.fromRGB(180, 0, 30) 
toggleButton.BackgroundTransparency = 0.2
toggleButton.TextColor3 = Color3.new(1, 1, 1) 
toggleButton.Font = Enum.Font.Code
toggleButton.TextSize = 12
toggleButton.BorderColor3 = Color3.fromRGB(255, 255, 255)
toggleButton.Parent = mainFrame 

local function updateList() 
    for _, child in pairs(scrollingFrame:GetChildren()) do if child:IsA("TextButton") then child:Destroy() end end 
    for _, p in pairs(Players:GetPlayers()) do 
        if p ~= player and (string.find(p.Name:lower(), textBox.Text:lower()) or string.find(p.DisplayName:lower(), textBox.Text:lower())) then 
            local btn = Instance.new("TextButton") 
            btn.Size = UDim2.new(1, 0, 0, 22) 
            btn.Text = " > " .. p.DisplayName .. " (@" .. p.Name .. ")"
            btn.BackgroundColor3 = table.find(selectedPlayers, p) and Color3.fromRGB(0, 100, 50) or Color3.fromRGB(15, 15, 15) 
            btn.BackgroundTransparency = 0.5
            btn.TextColor3 = table.find(selectedPlayers, p) and Color3.fromRGB(0, 255, 150) or Color3.fromRGB(200, 200, 200) 
            btn.TextSize = 11
            btn.Font = Enum.Font.RobotoMono
            btn.TextXAlignment = Enum.TextXAlignment.Left 
            btn.BorderColor3 = Color3.fromRGB(40, 0, 10)
            btn.Parent = scrollingFrame 
            
            btn.MouseButton1Click:Connect(function() 
                local index = table.find(selectedPlayers, p)
                if index then
                    table.remove(selectedPlayers, index)
                    btn.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
                    btn.TextColor3 = Color3.fromRGB(200, 200, 200)
                else
                    table.insert(selectedPlayers, p)
                    btn.BackgroundColor3 = Color3.fromRGB(0, 100, 50)
                    btn.TextColor3 = Color3.fromRGB(0, 255, 150)
                end
                
                if #selectedPlayers > 0 then
                    textBox.Text = "Targets Selected: " .. tostring(#selectedPlayers)
                else
                    textBox.Text = ""
                end
            end) 
        end 
    end 
end 

clearBtn.MouseButton1Click:Connect(function()
    selectedPlayers = {}
    textBox.Text = ""
    updateList()
    if isAttached then toggleButton:Click() end
end)

selectAllBtn.MouseButton1Click:Connect(function()
    selectedPlayers = {}
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= player then
            table.insert(selectedPlayers, p)
        end
    end
    textBox.Text = "Targets Selected: " .. tostring(#selectedPlayers)
    updateList()
end)

textBox:GetPropertyChangedSignal("Text"):Connect(function()
    if not string.find(textBox.Text, "Targets Selected:") then updateList() end
end) 
updateList() 

local function emergencyTargetShift()
    lastTargetSwitchTime = 0 
    currentTargetIndex = 1
    if #selectedPlayers > 0 then
        selectedPlayer = selectedPlayers[1]
    end
end

local function cleanUpPhysics()
    for _, conn in ipairs(_G.EndlessAuraStorage.Connections) do
        pcall(function() if conn then conn:Disconnect() end end)
    end
    _G.EndlessAuraStorage.Connections = {}
    
    if _G.EndlessAuraStorage.AnimForceConnection then 
        _G.EndlessAuraStorage.AnimForceConnection:Disconnect() 
        _G.EndlessAuraStorage.AnimForceConnection = nil 
    end

    disableAntivoid() -- Turn off protection when detaching

    pcall(function()
        local char = player.Character
        if char then
            local root = char:FindFirstChild("HumanoidRootPart")
            if sethiddenproperty and root then
                sethiddenproperty(root, "NetworkIsSleeping", false)
            end
            local hum = char:FindFirstChildOfClass("Humanoid")
            if hum then
                for _, track in pairs(hum:GetPlayingAnimationTracks()) do
                    track:Stop()
                end
                hum.PlatformStand = false
                hum:ChangeState(Enum.HumanoidStateType.Running)
            end
            local torso = char:FindFirstChild("Torso") or char:FindFirstChild("LowerTorso")
            local rootJoint = torso and (torso:FindFirstChild("RootJoint") or root:FindFirstChild("RootJoint"))
            if rootJoint then rootJoint.C0 = CFrame.new() end
        end
    end)
    activeTrack = nil
end

Players.PlayerRemoving:Connect(function(leftPlayer)
    local targetIdx = table.find(selectedPlayers, leftPlayer)
    if targetIdx then
        table.remove(selectedPlayers, targetIdx)
        emergencyTargetShift() 
        
        pcall(function()
            starterGui:SetCore("SendNotification", {
                Title = "Target Leave",
                Text = leftPlayer.DisplayName .. " left.",
                Duration = 4
            })
        end)
        
        if #selectedPlayers == 0 and isAttached then
            isAttached = false
            cleanUpPhysics()
            toggleButton.Text = "[ FUCK ON ]"
            toggleButton.BackgroundColor3 = Color3.fromRGB(180, 0, 30)
        end
    end
end)

-- ====================================================================
-- // SHADOW ATTACH ENGINE INTEGRATION // --
-- ====================================================================
toggleButton.MouseButton1Click:Connect(function() 
    if isAttached then 
        isAttached = false 
        cleanUpPhysics()
        toggleButton.Text = "[ FUCK ON ]" 
        toggleButton.BackgroundColor3 = Color3.fromRGB(180, 0, 30) 
    elseif #selectedPlayers > 0 then 
        isAttached = true 
        toggleButton.Text = "[ FUCK OFF ]" 
        toggleButton.BackgroundColor3 = Color3.fromRGB(30, 30, 30) 
        lastSwitchTime = tick()
        lastTargetSwitchTime = tick()
        currentTargetIndex = 1
        selectedPlayer = selectedPlayers[currentTargetIndex]
        currentAutoSubMode = "Normal"
        
        enableAntivoid() -- Active Antivoid On Attach Execution

        -- FORCE ANIMATION ENGINE
        _G.EndlessAuraStorage.AnimForceConnection = runService.Stepped:Connect(function()
            if not isAttached then return end
            pcall(function()
                local char = player.Character
                local hum = char and char:FindFirstChildOfClass("Humanoid")
                if hum and hum.Health > 0 then
                    local targetAnimToUse = bangAnimId
                    local currentModeToRun = targetMode
                    if targetMode == "Auto Switch" then
                        currentModeToRun = currentAutoSubMode
                    end

                    if currentModeToRun == "Face Bang" then
                        targetAnimToUse = faceBangAnimId
                    else
                        targetAnimToUse = bangAnimId
                    end

                    for _, track in pairs(hum:GetPlayingAnimationTracks()) do
                        if track.Animation.AnimationId ~= targetAnimToUse then
                            track:Stop()
                        end
                    end

                    if not activeTrack or activeTrack.Animation.AnimationId ~= targetAnimToUse or not activeTrack.IsPlaying then
                        if activeTrack then pcall(function() activeTrack:Stop() end) end
                        local newAnim = Instance.new("Animation")
                        newAnim.AnimationId = targetAnimToUse
                        activeTrack = hum:LoadAnimation(newAnim)
                        activeTrack.Priority = Enum.AnimationPriority.Action4
                        activeTrack.Looped = true
                        activeTrack:Play()
                    end
                    
                    if activeTrack then
                        activeTrack:AdjustSpeed(speedMultiplier / 5)
                    end
                end
            end)
        end)

        -- POSITION SYNCHRONIZER (PERSISTENT LOOP MODIFICATION)
        local function syncPosition()
            if not isAttached or #selectedPlayers == 0 then return end
            
            if tick() - lastTargetSwitchTime >= 0.1 or not selectedPlayer then
                lastTargetSwitchTime = tick()
                currentTargetIndex = currentTargetIndex + 1
                if currentTargetIndex > #selectedPlayers then currentTargetIndex = 1 end
                selectedPlayer = selectedPlayers[currentTargetIndex]
            end

            if not selectedPlayer or not selectedPlayer.Character then return end

            local myChar = player.Character
            local myHRP = myChar and myChar:FindFirstChild("HumanoidRootPart") 
            local myHumanoid = myChar and myChar:FindFirstChild("Humanoid")
            
            local targetHRP = selectedPlayer.Character:FindFirstChild("HumanoidRootPart")
            local tOrient = getOrientationPart(selectedPlayer.Character)
            local targetHead = selectedPlayer.Character:FindFirstChild("Head") 

            -- MODIFIED: Target ke marne ya part miss hone par bhi script self-disable nahi hogi!
            if not myHRP or not myHumanoid or not targetHRP or not tOrient then 
                return -- Bus next cycle ka wait karega, toggle off nahi karega.
            end

            local currentModeToRun = targetMode
            if targetMode == "Auto Switch" then
                if tick() - lastSwitchTime >= 1.0 then
                    lastSwitchTime = tick()
                    currentAutoSubMode = (currentAutoSubMode == "Normal") and "Face Bang" or "Normal"
                end
                currentModeToRun = currentAutoSubMode
            end

            pcall(function() myHumanoid.PlatformStand = true end) 
            
            -- STROKE OSCILLATION CALCULATION
            local dynamicOffset = 0
            local sineWave = 0
            local tiltAngle = 0
            
            if attachDistance > 0 then
                local timeFactor = tick() * speedMultiplier
                sineWave = math.sin(timeFactor)
                dynamicOffset = math.abs(sineWave) * forwardBackRange
                tiltAngle = -sineWave * 0.15
            end

            local targetCFrame
            if currentModeToRun == "Face Bang" then
                local basePartForRotation = targetHead or tOrient
                targetCFrame = basePartForRotation.CFrame 
                    * CFrame.new(0, fixedUpOffset, -attachDistance - dynamicOffset - 0.2) 
                    * CFrame.Angles(0, math.pi, 0)
                    + basePartForRotation.CFrame.LookVector * (sineWave * 0.7)
            else
                targetCFrame = tOrient.CFrame 
                    * CFrame.new(0, 0, attachDistance + dynamicOffset + 0.8 + (attachDistance > 0 and sineWave or 0)) 
                    * CFrame.Angles(tiltAngle + math.rad(sineWave * 15), 0, 0)
            end
            
            -- SHADOW GLITCH LOOPER METHOD
            pcall(function()
                if sethiddenproperty then
                    sethiddenproperty(myHRP, "NetworkIsSleeping", true)
                end
                myHRP.CFrame = targetCFrame * CFrame.new(TARGET_OFFSET)
                myHRP.Velocity = Vector3.new(0, 0.03, 0)
                myHRP.AssemblyLinearVelocity = Vector3.new(0, 0.03, 0)
                myHRP.AssemblyAngularVelocity = Vector3.new(0, 0, 0)
            end)
            
            pcall(function()
                local torso = myChar:FindFirstChild("Torso") or myChar:FindFirstChild("LowerTorso")
                local rootJoint = torso and (torso:FindFirstChild("RootJoint") or myHRP:FindFirstChild("RootJoint"))
                if rootJoint and attachDistance > 0 then
                    local hipThrustAngle = math.rad(sineWave * (forwardBackRange * 15))
                    local jointOffsetZ = -math.abs(sineWave) * (forwardBackRange * 0.2)
                    rootJoint.C0 = CFrame.new(0, 0, jointOffsetZ) * CFrame.Angles((currentModeToRun == "Face Bang" and hipThrustAngle or -hipThrustAngle), 0, 0)
                elseif rootJoint then
                    rootJoint.C0 = CFrame.new()
                end
            end)
            
            pcall(function()
                for _, v in pairs(myChar:GetDescendants()) do
                    if v:IsA("BasePart") then v.CanCollide = false end
                end
            end)
        end

        table.insert(_G.EndlessAuraStorage.Connections, runService.PreSimulation:Connect(syncPosition))
        table.insert(_G.EndlessAuraStorage.Connections, runService.RenderStepped:Connect(syncPosition))
    end 
end)

-- Core Notification System
starterGui:SetCore("SendNotification", {
    Title = "Antivoid Loaded",
    Text = "Void Protection (NaN Logic) successfully integrated into pipeline.",
    Duration = 5
})
